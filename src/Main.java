
public class Main {

	public static void main(String[] args) {
		
		LaunchPage page = new LaunchPage();
	}
	
	// To sum up everything, if you want to open a new window, you have to create a instance of the class that
	// contains the window that you want to appear, and than you want to hook this up with some sort 
	// of a trigger event, to trigger it, in that case, is a button with a action listener interface implemented
	// on it's frame, executing the AcionPerformed method, creating a instance of the window that you o 
	// want to appear.
	
	// that's just one of the few ways to do it.

}
